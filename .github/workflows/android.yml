name: Vex Multi-Platform Universal Engine

on:
  repository_dispatch:
    types: [trigger-build, build-app]
  workflow_dispatch:

jobs:
  # =============================================================
  # 1. ANDROID ENGINE (APK & Play Store .AAB)
  # =============================================================
  build-android:
    if: |
      github.event.client_payload.platform == 'apk' || 
      github.event.client_payload.platform == 'aab' ||
      github.event.client_payload.targetPlatform == 'apk' || 
      github.event.client_payload.targetPlatform == 'aab' ||
      (!github.event.client_payload.platform && !github.event.client_payload.targetPlatform)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code Repository
        uses: actions/checkout@v4

      - name: Setup Java JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Flutter Framework
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Download & Extract Uploaded Custom Code / ZIP File (If Uploaded)
        run: |
          mkdir -p builder/android_template/assets/www
          FILE_URL="${{ github.event.client_payload.file_url }}"
          
          if [ -n "$FILE_URL" ] && [ "$FILE_URL" != "null" ]; then
            echo "[VEX ENGINE] Custom file uploaded by user. Downloading: $FILE_URL"
            curl -s -L -o uploaded_source.zip "$FILE_URL" || true
            
            # Check if file is ZIP archive or raw HTML
            if file uploaded_source.zip | grep -q 'Zip archive'; then
              echo "[VEX ENGINE] Extracting ZIP Archive into assets..."
              unzip -o uploaded_source.zip -d builder/android_template/assets/www/
            else
              echo "[VEX ENGINE] Setting single uploaded file as index..."
              cp uploaded_source.zip builder/android_template/assets/www/index.html
            fi
          else
            echo "[VEX ENGINE] No custom file uploaded. Using Web URL mode."
          fi

      - name: Inject License & Multi-Res User Icons
        run: |
          echo "Software License - Registered to Vex Engine" > builder/android_template/assets/LICENSE.txt
          
          curl -s -o user_icon.png "http://104.251.123.179:1747/user_icon.png" || true
          if [ -f user_icon.png ]; then
            sudo apt-get install -y imagemagick
            convert user_icon.png -resize 192x192 builder/android_template/android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png || true
            convert user_icon.png -resize 144x144 builder/android_template/android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png || true
            convert user_icon.png -resize 96x96 builder/android_template/android/app/src/main/res/mipmap-xhdpi/ic_launcher.png || true
            convert user_icon.png -resize 72x72 builder/android_template/android/app/src/main/res/mipmap-hdpi/ic_launcher.png || true
            convert user_icon.png -resize 48x48 builder/android_template/android/app/src/main/res/mipmap-mdpi/ic_launcher.png || true
          fi

      - name: Auto-Generate Keystore Certificate (No Warnings)
        working-directory: builder/android_template/android/app
        run: |
          keytool -genkeypair -v \
            -keystore vex-key.jks \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -alias vexkey \
            -dname "CN=VexApp, OU=Software, O=Vex, L=City, S=State, C=US" \
            -storepass "VexAppPass123" \
            -keypass "VexAppPass123"

      - name: Fetch Flutter Dependencies
        working-directory: builder/android_template
        run: flutter pub get

      - name: Compile Android Release Binary
        working-directory: builder/android_template
        run: |
          TARGET="${{ github.event.client_payload.platform || github.event.client_payload.targetPlatform }}"
          URL="${{ github.event.client_payload.app_url || github.event.client_payload.web_url }}"
          NAME="${{ github.event.client_payload.app_name }}"
          FILE_URL="${{ github.event.client_payload.file_url }}"

          if [ "$TARGET" = "aab" ]; then
            flutter build appbundle --release --dart-define=WEB_URL="$URL" --dart-define=APP_NAME="$NAME" --dart-define=FILE_URL="$FILE_URL"
          else
            flutter build apk --release --dart-define=WEB_URL="$URL" --dart-define=APP_NAME="$NAME" --dart-define=FILE_URL="$FILE_URL"
          fi

      - name: Process & Format Output Name
        run: |
          RAW_NAME="${{ github.event.client_payload.app_name || 'VexApp' }}"
          SAFE_NAME=$(echo "$RAW_NAME" | sed 's/[^a-zA-Z0-9_-]/_/g')
          TARGET="${{ github.event.client_payload.platform || github.event.client_payload.targetPlatform }}"

          if [ "$TARGET" = "aab" ]; then
            OUT_NAME="${SAFE_NAME}-playstore.aab"
            cp builder/android_template/build/app/outputs/bundle/release/app-release.aab "./$OUT_NAME"
          else
            OUT_NAME="${SAFE_NAME}-release.apk"
            cp builder/android_template/build/app/outputs/flutter-apk/app-release.apk "./$OUT_NAME"
          fi
          echo "TARGET_FILE=$OUT_NAME" >> $GITHUB_ENV

      - name: Push Compiled Binary to VPS Server
        run: |
          curl -X POST "http://104.251.123.179:1747/api/upload-build" \
            -H "x-file-name: ${{ env.TARGET_FILE }}" \
            --data-binary "@${{ env.TARGET_FILE }}"

  # =============================================================
  # 2. WINDOWS ENGINE (EXE & WINDOWS STORE INSTALLER)
  # =============================================================
  build-windows:
    if: |
      github.event.client_payload.platform == 'windows' || 
      github.event.client_payload.platform == 'exe' || 
      github.event.client_payload.platform == 'msix' ||
      github.event.client_payload.targetPlatform == 'windows' || 
      github.event.client_payload.targetPlatform == 'exe' ||
      github.event.client_payload.targetPlatform == 'msix'
    runs-on: windows-latest

    steps:
      - name: Checkout Code Repository
        uses: actions/checkout@v4

      - name: Setup Flutter Framework
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Enable Windows Desktop Support
        run: flutter config --enable-windows-desktop

      - name: Inject Missing Windows Runner Files
        working-directory: builder/android_template
        run: flutter create --platforms=windows .

      - name: Fetch & Process Custom Windows Icon
        shell: powershell
        run: |
          choco install imagemagick -y
          Invoke-WebRequest -Uri "http://104.251.123.179:1747/user_icon.png" -OutFile "user_icon.png" -ErrorAction SilentlyContinue
          if (Test-Path "user_icon.png") {
            magick convert user_icon.png -define icon:auto-resize="256,128,64,48,32,16" builder/android_template/windows/runner/resources/app_icon.ico
          }

      - name: Fetch Flutter Dependencies
        working-directory: builder/android_template
        run: flutter pub get

      - name: Build Windows Executable
        working-directory: builder/android_template
        env:
          CXXFLAGS: "/D_SILENCE_EXPERIMENTAL_COROUTINE_DEPRECATION_WARNINGS"
          CFLAGS: "/D_SILENCE_EXPERIMENTAL_COROUTINE_DEPRECATION_WARNINGS"
        run: |
          $url = "${{ github.event.client_payload.app_url || github.event.client_payload.web_url }}"
          $name = "${{ github.event.client_payload.app_name }}"
          $fileUrl = "${{ github.event.client_payload.file_url }}"
          flutter build windows --release --dart-define=WEB_URL="$url" --dart-define=APP_NAME="$name" --dart-define=FILE_URL="$fileUrl"

      - name: Generate Certificate & Build Windows Installer (.EXE)
        shell: powershell
        run: |
          choco install innosetup -y
          
          $rawName = "${{ github.event.client_payload.app_name }}"
          if ([string]::IsNullOrWhiteSpace($rawName)) { $rawName = "VexApp" }
          $safeName = $rawName -replace '[^a-zA-Z0-9_-]', '_'
          
          # Detect Built Exe Name dynamically
          $exePath = Get-ChildItem -Path "builder\android_template\build\windows\x64\runner\Release\*.exe" | Select-Object -First 1 -ExpandProperty Name
          if ([string]::IsNullOrWhiteSpace($exePath)) { $exePath = "builder.exe" }
          
          # Self-Signed Windows Certificate
          $cert = New-SelfSignedCertificate -Type Custom -Subject "CN=$safeName Publisher, O=Vex Engine" -KeyUsage DigitalSignature -FriendlyName "$safeName Cert" -CertStoreLocation "Cert:\CurrentUser\My"
          
          $issContent = @"
          [Setup]
          AppName=$safeName
          AppVersion=1.0
          AppPublisher=Vex Software Engine
          DefaultDirName={autopf}\$safeName
          DefaultGroupName=$safeName
          OutputDir=.
          OutputBaseFilename=$safeName-Setup
          Compression=lzma2/ultra64
          SolidCompression=yes

          [Files]
          Source: "builder\android_template\build\windows\x64\runner\Release\*"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs createallsubdirs

          [Icons]
          Name: "{autoprograms}\$safeName"; Filename: "{app}\$exePath"
          Name: "{commondesktop}\$safeName"; Filename: "{app}\$exePath"

          [Run]
          Filename: "{app}\$exePath"; Description: "Launch App"; Flags: nowait postinstall skipifsilent
          "@
          
          Set-Content -Path "installer.iss" -Value $issContent
          ISCC.exe installer.iss

      - name: Push Windows Setup to VPS Server
        shell: bash
        run: |
          RAW_NAME="${{ github.event.client_payload.app_name || 'VexApp' }}"
          SAFE_NAME=$(echo "$RAW_NAME" | sed 's/[^a-zA-Z0-9_-]/_/g')
          EXE_NAME="${SAFE_NAME}-Setup.exe"
          
          curl -X POST "http://104.251.123.179:1747/api/upload-build" \
            -H "x-file-name: $EXE_NAME" \
            --data-binary "@$EXE_NAME"

  # =============================================================
  # 3. iOS ENGINE (APP STORE .IPA)
  # =============================================================
  build-ios:
    if: |
      github.event.client_payload.platform == 'ipa' || 
      github.event.client_payload.targetPlatform == 'ipa'
    runs-on: macos-latest

    steps:
      - name: Checkout Code Repository
        uses: actions/checkout@v4

      - name: Setup Flutter Framework
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Fetch Dependencies
        working-directory: builder/android_template
        run: flutter pub get

      - name: Build iOS IPA Package
        working-directory: builder/android_template
        run: |
          URL="${{ github.event.client_payload.app_url || github.event.client_payload.web_url }}"
          NAME="${{ github.event.client_payload.app_name }}"
          FILE_URL="${{ github.event.client_payload.file_url }}"
          flutter build ios --release --no-codesign --dart-define=WEB_URL="$URL" --dart-define=APP_NAME="$NAME" --dart-define=FILE_URL="$FILE_URL"
          
          RAW_NAME="${{ github.event.client_payload.app_name || 'VexApp' }}"
          SAFE_NAME=$(echo "$RAW_NAME" | sed 's/[^a-zA-Z0-9_-]/_/g')
          
          mkdir -p Payload
          mv build/ios/iphoneos/Runner.app Payload/
          zip -r "${SAFE_NAME}-iOS.ipa" Payload
          echo "IPA_FILE=${SAFE_NAME}-iOS.ipa" >> $GITHUB_ENV

      - name: Push iOS IPA Package to VPS Server
        run: |
          curl -X POST "http://104.251.123.179:1747/api/upload-build" \
            -H "x-file-name: ${{ env.IPA_FILE }}" \
            --data-binary "@${{ env.IPA_FILE }}"
